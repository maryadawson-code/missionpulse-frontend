# =============================================================================
# MissionPulse — Next.js Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local — it is gitignored.
#
# Mission Meets Tech — Mission. Technology. Transformation.
# =============================================================================

# -----------------------------------------------------------------------------
# SUPABASE (Required)
# -----------------------------------------------------------------------------
# Get these from: Supabase Dashboard → Settings → API
# Instance: djuviwarqdvlbgcfuupa

# Public — safe for browser (included in client bundle)
NEXT_PUBLIC_SUPABASE_URL=https://djuviwarqdvlbgcfuupa.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Private — server-only (NEVER prefix with NEXT_PUBLIC_)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# -----------------------------------------------------------------------------
# SITE URL (Required)
# -----------------------------------------------------------------------------
# Used for auth redirects. Must match your deployment URL.
# Local: http://localhost:3000
# Staging: https://v2-development--missionpulse.netlify.app
# Production: https://missionpulse.netlify.app (or missionpulse.io when DNS ready)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# AI SERVICES (Optional — for AI agent features)
# -----------------------------------------------------------------------------
ANTHROPIC_API_KEY=sk-ant-your-key-here

# -----------------------------------------------------------------------------
# EXTERNAL APIS (Optional — Phase 2+)
# -----------------------------------------------------------------------------
# SAM_GOV_API_KEY=your-sam-gov-key
# HUBSPOT_API_KEY=your-hubspot-key

# -----------------------------------------------------------------------------
# FEATURE FLAGS (Optional)
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_ENABLE_AI_CHAT=false
# NEXT_PUBLIC_ENABLE_PARTNER_ACCESS=false
# NEXT_PUBLIC_ENABLE_MFA=false
